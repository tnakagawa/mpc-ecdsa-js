import * as _ from 'lodash';

export function split(_secret: number, _n: number, _k: number): number[] {
  // TODO: Implement me
  return new Array<number>(_n);
};

export function reconstruct(_shares: number[], _n: number, _k: number): number {
  // TODO: Implement me
  return 0;
};
